/*
 * Docker Engine API
 * The Engine API is an HTTP API served by Docker Engine. It is the API the Docker client uses to communicate with the Engine, so everything the Docker client can do can be done with the API.  Most of the client's commands map directly to API endpoints (e.g. `docker ps` is `GET /containers/json`). The notable exception is running containers, which consists of several API calls.  # Errors  The API uses standard HTTP status codes to indicate the success or failure of the API call. The body of the response will be JSON in the following format:  ``` {   \"message\": \"page not found\" } ```  # Versioning  The API is usually changed in each release, so API calls are versioned to ensure that clients don't break. To lock to a specific version of the API, you prefix the URL with its version, for example, call `/v1.30/info` to use the v1.30 version of the `/info` endpoint. If the API version specified in the URL is not supported by the daemon, a HTTP `400 Bad Request` error message is returned.  If you omit the version-prefix, the current version of the API (v1.41) is used. For example, calling `/info` is the same as calling `/v1.41/info`. Using the API without a version-prefix is deprecated and will be removed in a future release.  Engine releases in the near future should support this version of the API, so your client will continue to work even if it is talking to a newer Engine.  The API uses an open schema model, which means server may add extra properties to responses. Likewise, the server will ignore any extra query parameters and request body properties. When you write clients, you need to ignore additional properties in responses to ensure they do not break when talking to newer daemons.   # Authentication  Authentication for registries is handled client side. The client has to send authentication details to various endpoints that need to communicate with registries, such as `POST /images/(name)/push`. These are sent as `X-Registry-Auth` header as a [base64url encoded](https://tools.ietf.org/html/rfc4648#section-5) (JSON) string with the following structure:  ``` {   \"username\": \"string\",   \"password\": \"string\",   \"email\": \"string\",   \"serveraddress\": \"string\" } ```  The `serveraddress` is a domain/IP without a protocol. Throughout this structure, double quotes are required.  If you have already got an identity token from the [`/auth` endpoint](#operation/SystemAuth), you can just pass this instead of credentials:  ``` {   \"identitytoken\": \"9cbaf023786cd7...\" } ``` 
 *
 * OpenAPI spec version: 1.41
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.21
 *
 * Do not edit the class manually.
 *
 */

(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD.
    define(['expect.js', '../../src/index'], factory);
  } else if (typeof module === 'object' && module.exports) {
    // CommonJS-like environments that support module.exports, like Node.
    factory(require('expect.js'), require('../../src/index'));
  } else {
    // Browser globals (root is window)
    factory(root.expect, root.DockerEngineApi);
  }
}(this, function(expect, DockerEngineApi) {
  'use strict';

  var instance;

  beforeEach(function() {
    instance = new DockerEngineApi.ExecApi();
  });

  describe('(package)', function() {
    describe('ExecApi', function() {
      describe('containerExec', function() {
        it('should call containerExec successfully', function(done) {
          // TODO: uncomment, update parameter values for containerExec call and complete the assertions
          /*
          var execConfig = new DockerEngineApi.ExecConfig();
          execConfig.attachStdin = false;
          execConfig.attachStdout = false;
          execConfig.attachStderr = false;
          execConfig.detachKeys = "";
          execConfig.tty = false;
          execConfig.env = [""];
          execConfig.cmd = [""];
          execConfig.privileged = false;
          execConfig.user = "";
          execConfig.workingDir = "";
          var id = "id_example";

          instance.containerExec(execConfig, id, function(error, data, response) {
            if (error) {
              done(error);
              return;
            }
            // TODO: update response assertions
            expect(data).to.be.a(DockerEngineApi.IdResponse);
            expect(data.id).to.be.a('string');
            expect(data.id).to.be("");

            done();
          });
          */
          // TODO: uncomment and complete method invocation above, then delete this line and the next:
          done();
        });
      });
      describe('execInspect', function() {
        it('should call execInspect successfully', function(done) {
          // TODO: uncomment, update parameter values for execInspect call and complete the assertions
          /*
          var id = "id_example";

          instance.execInspect(id, function(error, data, response) {
            if (error) {
              done(error);
              return;
            }
            // TODO: update response assertions
            expect(data).to.be.a(DockerEngineApi.ExecInspectResponse);
            expect(data.canRemove).to.be.a('boolean');
            expect(data.canRemove).to.be(false);
            expect(data.detachKeys).to.be.a('string');
            expect(data.detachKeys).to.be("");
            expect(data.ID).to.be.a('string');
            expect(data.ID).to.be("");
            expect(data.running).to.be.a('boolean');
            expect(data.running).to.be(false);
            expect(data.exitCode).to.be.a('number');
            expect(data.exitCode).to.be(0);
            expect(data.processConfig).to.be.a(DockerEngineApi.ProcessConfig);
                  expect(data.processConfig.privileged).to.be.a('boolean');
              expect(data.processConfig.privileged).to.be(false);
              expect(data.processConfig.user).to.be.a('string');
              expect(data.processConfig.user).to.be("");
              expect(data.processConfig.tty).to.be.a('boolean');
              expect(data.processConfig.tty).to.be(false);
              expect(data.processConfig.entrypoint).to.be.a('string');
              expect(data.processConfig.entrypoint).to.be("");
              {
                let dataCtr = data.processConfig._arguments;
                expect(dataCtr).to.be.an(Array);
                expect(dataCtr).to.not.be.empty();
                for (let p in dataCtr) {
                  let data = dataCtr[p];
                  expect(data).to.be.a('string');
                  expect(data).to.be("");
                }
              }
            expect(data.openStdin).to.be.a('boolean');
            expect(data.openStdin).to.be(false);
            expect(data.openStderr).to.be.a('boolean');
            expect(data.openStderr).to.be(false);
            expect(data.openStdout).to.be.a('boolean');
            expect(data.openStdout).to.be(false);
            expect(data.containerID).to.be.a('string');
            expect(data.containerID).to.be("");
            expect(data.pid).to.be.a('number');
            expect(data.pid).to.be(0);

            done();
          });
          */
          // TODO: uncomment and complete method invocation above, then delete this line and the next:
          done();
        });
      });
      describe('execResize', function() {
        it('should call execResize successfully', function(done) {
          // TODO: uncomment, update parameter values for execResize call
          /*
          var id = "id_example";
          var opts = {};
          opts.h = 56;
          opts.w = 56;

          instance.execResize(id, opts, function(error, data, response) {
            if (error) {
              done(error);
              return;
            }

            done();
          });
          */
          // TODO: uncomment and complete method invocation above, then delete this line and the next:
          done();
        });
      });
      describe('execStart', function() {
        it('should call execStart successfully', function(done) {
          // TODO: uncomment, update parameter values for execStart call
          /*
          var id = "id_example";
          var opts = {};
          opts.execStartConfig = new DockerEngineApi.ExecStartConfig();
          opts.execStartConfig.detach = false;
          opts.execStartConfig.tty = false;

          instance.execStart(id, opts, function(error, data, response) {
            if (error) {
              done(error);
              return;
            }

            done();
          });
          */
          // TODO: uncomment and complete method invocation above, then delete this line and the next:
          done();
        });
      });
    });
  });

}));
